apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bb-chart.fullname" . }}-test-connection"
  labels:
    {{- include "bb-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bb-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
