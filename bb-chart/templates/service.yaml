apiVersion: v1
kind: Service
metadata:
  name: {{ include "bb-chart.fullname" . }}
  labels:
    {{- include "bb-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bb-chart.selectorLabels" . | nindent 4 }}
